<% if @user.errors.any? %>
	<h3>The form contains <%= pluralize(@user.errors.count, "error") %> </h3>
	<ul>
	<% @user.errors.full_messages.each do |err| %>
		<li><%= err %></li>
	<% end %>
	</ul>
<% end %>