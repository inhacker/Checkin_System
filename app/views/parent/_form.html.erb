<%= form_for(@parent) do |f| %>
	<% if @parent.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@parent.errors.count, "error") %> prohibited this parent from being saved:</h2>

      <ul>
      <% @parent.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

	<input type="hidden" name="kid_in_progress" value="<%= @kid_in_progress %>" />

  
	<div class="field">
		<%= f.label :f_name, "First Name" %><br>
    <%= f.text_field :f_name %>
	</div>
	<div class="field">
		<%= f.label :m_name, "Middle Name" %><br>
    <%= f.text_field :m_name %>
	</div>
	<div class="field">
		<%= f.label :l_name, "Last Name" %><br>
    <%= f.text_field :l_name %>
	</div>
	<div class="field">
		<%= f.label :email, "Email" %><br>
    <%= f.text_field :email %>
	</div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
